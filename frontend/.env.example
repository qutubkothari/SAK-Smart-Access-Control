# Frontend Environment Variables
# For local development:
# VITE_API_URL=http://localhost:3000/api/v1

# For EC2 deployment (behind Nginx proxy):
# Use relative URL so builds work across IP changes
VITE_API_URL=/api/v1

# Optional override for WebSocket endpoint (defaults to same origin)
# VITE_SOCKET_URL=http://3.108.52.219
